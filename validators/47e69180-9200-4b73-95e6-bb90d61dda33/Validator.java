
public class Validator {
   /**
	* Method to validate a commit and rturns true if the commit details are valid other wise it returns false
	@param  data the commit data to verify
	@return boolean indicating whther or not the commit was valid
    */
	public boolean validate(String data) {
		System.out.println(data);
		return true;
	}
}
